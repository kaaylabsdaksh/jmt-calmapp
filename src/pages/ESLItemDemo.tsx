import FormVariationsDemo from "./FormVariationsDemo";

// ESL Items branch — separate route from /form-variations so ESL can evolve independently.
const ESLItemDemo = () => {
  return <FormVariationsDemo />;
};

export default ESLItemDemo;
